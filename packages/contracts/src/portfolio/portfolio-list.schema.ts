import { ListQuerySchema, createListResponseSchema } from "../common/list.contract";

export const PortfolioListQuerySchema = ListQuerySchema;
export const createPortfolioListResponseSchema = createListResponseSchema;
